# Betting ToDo List - Vue

## サーバーの実行

nodejs は最新バージョン。

```bash
$ npm install && npm run dev
```

## ファイル構成

```bash
/src
  --/assets
     --画像とか
  --/pages
     --CreateTask.vue
     --Home.vue
     --Login.vue
     --Signup.vue
     --Mypage.vue
```
